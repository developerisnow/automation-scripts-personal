# 🖱️ iTerm2 + TMUX Mouse Settings

## ✅ Правильные настройки iTerm2:

### 1. **Preferences → Profiles → Terminal**
- [x] Enable mouse reporting
- [x] Report mouse wheel events  
- [x] Report mouse clicks & drags

### 2. **Preferences → General → Selection**
- [x] Applications in terminal may access clipboard
- [ ] Copy to pasteboard on selection (выключи!)

### 3. **Preferences → Advanced**
Search "mouse" и проверь:
- Mouse reporting: Normal
- Scroll wheel sends arrow keys: No

## 🎯 Тест что все работает:

```bash
# 1. Создай тестовую сессию
tmux new -s test
tmux split-window -h
seq 1 1000  # Генерируем длинный вывод

# 2. Проверь:
# - Клик мышкой переключает панели ✓
# - Scroll wheel прокручивает ✓
# - Option + Scroll = быстрая прокрутка ✓
```

## 🚨 Частые проблемы:

| Симптом | Причина | Фикс |
|---------|---------|------|
| Scroll двигает историю команд | tmux не в mouse mode | `tmux set -g mouse on` |
| Выделяет текст вместо скролла | iTerm2 перехватывает | Проверь настройки выше |
| Scroll работает только в одной панели | Нужно кликнуть для фокуса | Кликни мышкой на панель |
| Вообще ничего не работает | Старая версия tmux | `brew upgrade tmux` |

## 💡 Pro Tips:

### Альтернативный способ скролла:
1. `Ctrl+Space` + `[` - войти в copy mode
2. Используй стрелки или PageUp/PageDown
3. `q` - выйти

### Если хочешь временно отключить mouse:
```bash
# Отключить
tmux set -g mouse off

# Включить обратно  
tmux set -g mouse on
```

### Копирование с mouse:
1. **Зажми Option** + выдели мышкой
2. **Cmd+C** для копирования
3. Это обходит tmux и работает напрямую через iTerm2
